1. Выполнить npm i
2. Включить в WS prettier и eslint
3. Установить плагины npm install  eslint vite-plugin-eslint eslint-config-react-app --save-dev
4. Добавить файлы .prettierrc и .eslintrc.json

    Prettier config:
    {
      "singleQuote": true,
      "arrowParens": "avoid"
    }

    Eslint config:
    {
      "extends": "react-app"
    }

5. Установить vite.config.js как:

    import { defineConfig } from 'vite';
    import react from '@vitejs/plugin-react';
    import eslint from 'vite-plugin-eslint';

    // https://vitejs.dev/config/
    export default defineConfig({
      plugins: [react(), eslint()],
    });

